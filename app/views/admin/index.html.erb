Click on the link to see the list of Project <%= link_to "Project", projects_path %> 
<br />
Click on the link to see the list of Group <%= link_to "Groups", '/admin/groups' %> 
<br />

Student Lists

<table>
  <tr>
    <th></th>
    <th>Snumber</th>
    <th>Name</th>
    <th>Surname</th>
    <th>Email</th>
    <th></th>
  </tr>
<%= form_tag( controller:"admin", action:"creategroup") do %>
<% @student.each do |student| %>
  <tr>
    <td><%= check_box_tag "student_ids[]", student.id %></td>
    <td><%= student.snumber %></td>
    <td><%= student.name %></td>
    <td><%= student.surname %></td>
    <td><%= student.email %></td>
  </tr>
<% end %>
<%= text_field_tag(:name) %>
<%= submit_tag "Create" %>
</table>
<% end %>
