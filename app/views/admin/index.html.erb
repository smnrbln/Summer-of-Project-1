Proje listesi için <%= link_to "Proje", projes_path %> linkini tıklayınız.
<br />
Öğrenci gruplarını görmek için <%= link_to "Gruplar", '/admin/groups' %> tıklayınız.
<br />

Öğrenci listesi

<table>
  <tr>
    <th></th>
    <th>No</th>
    <th>Ad</th>
    <th>Soyad</th>
    <th>Email</th>
    <th></th>
  </tr>
<%= form_tag( controller:"admin", action:"creategroup") do %>
<% @ogrenci.each do |ogrenci| %>
  <tr>
    <td><%= check_box_tag "ogrenci_ids[]", ogrenci.id %></td>
    <td><%= ogrenci.okulno %></td>
    <td><%= ogrenci.ad %></td>
    <td><%= ogrenci.soyad %></td>
    <td><%= ogrenci.email %></td>
  </tr>
<% end %>
<%= text_field_tag(:name) %>
<%= submit_tag "Oluştur" %>
</table>
<% end %>
