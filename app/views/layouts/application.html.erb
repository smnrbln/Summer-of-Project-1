<!DOCTYPE html>
<html>
<head>
  <title><%= t 'welcome.title' %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t 'welcome.languages' %><b class="caret"></b></a>
  <ul class="dropdown-menu">
    <li><a href="?locale=tr">Türkçe</a></li>
    <li class="divider"></li>
    <li><a href="?locale=en">English</a></li>
    <li><a href="?locale=ru">Русский</a></li>
  </ul>
  </li>
  <p>
     <% if @current_user  %>
      (: WELCOME..  <b><%= @current_user.username %></b> |
      <%= link_to "ÇIKIŞ", logout_url %>
    <% else %>
      Have not logged in..! |
      <%= link_to "GİRİŞ", login_url %>
    <% end %>
  </p>
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>
<%= yield %>

</body>
</html>
