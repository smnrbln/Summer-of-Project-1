<%= form_for(@proje) do |f| %>
  <% if @proje.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proje.errors.count, "error") %> İlgili proje kaydedilemez:</h2>

      <ul>
      <% @proje.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
